package entity.base;

/**
 * The interface Movable.
 */
public interface Movable {
    /**
     * Move boolean.
     *
     * @param dx the dx
     * @param dy the dy
     * @return the boolean
     */
    boolean move(int dx, int dy);
}
